import styles from "@/styles/Filter.module.css"

function Filter() {
    return (
        <div className={styles.filter}>
            <p>Filter Element</p>
        </div>
    )
}

export default Filter;